import styled from "styled-components";
import { greenMain, greyMain } from "../../colors/colorsMain";

export const Footer = styled.footer`
    display: flex;
    align-items: center;
    justify-content: center;
    background: ${greyMain};
    color: ${greenMain};
    padding: 20px;
`