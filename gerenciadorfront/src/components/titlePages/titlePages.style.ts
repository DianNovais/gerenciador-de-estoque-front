import styled from "styled-components"
import { greenFont, greenMain } from "../../colors/colorsMain"

const TitleContent = styled.h2`
    margin-bottom: 10px;
    color: ${greenMain};
`


export default TitleContent;